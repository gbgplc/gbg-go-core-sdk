/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

export type ClientCredentialsGrantRequest = {
  /**
   * The client ID assigned to your organization.
   */
  clientId: string;
  /**
   * The client secret assigned to your organization.
   */
  clientSecret: string;
  /**
   * Must always be set to 'client_credentials'.
   */
  grantType: "client_credentials";
};

/** @internal */
export type ClientCredentialsGrantRequest$Outbound = {
  client_id: string;
  client_secret: string;
  grant_type: "client_credentials";
};

/** @internal */
export const ClientCredentialsGrantRequest$outboundSchema: z.ZodMiniType<
  ClientCredentialsGrantRequest$Outbound,
  ClientCredentialsGrantRequest
> = z.pipe(
  z.object({
    clientId: z.string(),
    clientSecret: z.string(),
    grantType: z.literal("client_credentials"),
  }),
  z.transform((v) => {
    return remap$(v, {
      clientId: "client_id",
      clientSecret: "client_secret",
      grantType: "grant_type",
    });
  }),
);

export function clientCredentialsGrantRequestToJSON(
  clientCredentialsGrantRequest: ClientCredentialsGrantRequest,
): string {
  return JSON.stringify(
    ClientCredentialsGrantRequest$outboundSchema.parse(
      clientCredentialsGrantRequest,
    ),
  );
}
