/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gbg.gocore.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ConsumeInputSpec {

    public static final ConsumeInputSpec REQUIRED = new ConsumeInputSpec("required");
    public static final ConsumeInputSpec OPTIONAL = new ConsumeInputSpec("optional");
    public static final ConsumeInputSpec CONDITIONAL = new ConsumeInputSpec("conditional");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ConsumeInputSpec> values = createValuesMap();
    private static final Map<String, ConsumeInputSpecEnum> enums = createEnumsMap();

    private final String value;

    private ConsumeInputSpec(String value) {
        this.value = value;
    }

    /**
     * Returns a ConsumeInputSpec with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ConsumeInputSpec
     */ 
    @JsonCreator
    public static ConsumeInputSpec of(String value) {
        synchronized (ConsumeInputSpec.class) {
            return values.computeIfAbsent(value, v -> new ConsumeInputSpec(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ConsumeInputSpecEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ConsumeInputSpec other = (ConsumeInputSpec) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ConsumeInputSpec [value=" + value + "]";
    }

    // return an array just like an enum
    public static ConsumeInputSpec[] values() {
        synchronized (ConsumeInputSpec.class) {
            return values.values().toArray(new ConsumeInputSpec[] {});
        }
    }

    private static final Map<String, ConsumeInputSpec> createValuesMap() {
        Map<String, ConsumeInputSpec> map = new LinkedHashMap<>();
        map.put("required", REQUIRED);
        map.put("optional", OPTIONAL);
        map.put("conditional", CONDITIONAL);
        return map;
    }

    private static final Map<String, ConsumeInputSpecEnum> createEnumsMap() {
        Map<String, ConsumeInputSpecEnum> map = new HashMap<>();
        map.put("required", ConsumeInputSpecEnum.REQUIRED);
        map.put("optional", ConsumeInputSpecEnum.OPTIONAL);
        map.put("conditional", ConsumeInputSpecEnum.CONDITIONAL);
        return map;
    }
    
    
    public enum ConsumeInputSpecEnum {

        REQUIRED("required"),
        OPTIONAL("optional"),
        CONDITIONAL("conditional"),;

        private final String value;

        private ConsumeInputSpecEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

