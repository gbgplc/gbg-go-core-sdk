/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gbg.gocore.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ConsumeCombinator {

    public static final ConsumeCombinator ONE_OF = new ConsumeCombinator("oneOf");
    public static final ConsumeCombinator ALL_OF = new ConsumeCombinator("allOf");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ConsumeCombinator> values = createValuesMap();
    private static final Map<String, ConsumeCombinatorEnum> enums = createEnumsMap();

    private final String value;

    private ConsumeCombinator(String value) {
        this.value = value;
    }

    /**
     * Returns a ConsumeCombinator with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ConsumeCombinator
     */ 
    @JsonCreator
    public static ConsumeCombinator of(String value) {
        synchronized (ConsumeCombinator.class) {
            return values.computeIfAbsent(value, v -> new ConsumeCombinator(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ConsumeCombinatorEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ConsumeCombinator other = (ConsumeCombinator) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ConsumeCombinator [value=" + value + "]";
    }

    // return an array just like an enum
    public static ConsumeCombinator[] values() {
        synchronized (ConsumeCombinator.class) {
            return values.values().toArray(new ConsumeCombinator[] {});
        }
    }

    private static final Map<String, ConsumeCombinator> createValuesMap() {
        Map<String, ConsumeCombinator> map = new LinkedHashMap<>();
        map.put("oneOf", ONE_OF);
        map.put("allOf", ALL_OF);
        return map;
    }

    private static final Map<String, ConsumeCombinatorEnum> createEnumsMap() {
        Map<String, ConsumeCombinatorEnum> map = new HashMap<>();
        map.put("oneOf", ConsumeCombinatorEnum.ONE_OF);
        map.put("allOf", ConsumeCombinatorEnum.ALL_OF);
        return map;
    }
    
    
    public enum ConsumeCombinatorEnum {

        ONE_OF("oneOf"),
        ALL_OF("allOf"),;

        private final String value;

        private ConsumeCombinatorEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

