/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gbg.gocore.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class SubmitInteractionStatusError {

    public static final SubmitInteractionStatusError ERROR = new SubmitInteractionStatusError("error");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, SubmitInteractionStatusError> values = createValuesMap();
    private static final Map<String, SubmitInteractionStatusErrorEnum> enums = createEnumsMap();

    private final String value;

    private SubmitInteractionStatusError(String value) {
        this.value = value;
    }

    /**
     * Returns a SubmitInteractionStatusError with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as SubmitInteractionStatusError
     */ 
    @JsonCreator
    public static SubmitInteractionStatusError of(String value) {
        synchronized (SubmitInteractionStatusError.class) {
            return values.computeIfAbsent(value, v -> new SubmitInteractionStatusError(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<SubmitInteractionStatusErrorEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        SubmitInteractionStatusError other = (SubmitInteractionStatusError) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "SubmitInteractionStatusError [value=" + value + "]";
    }

    // return an array just like an enum
    public static SubmitInteractionStatusError[] values() {
        synchronized (SubmitInteractionStatusError.class) {
            return values.values().toArray(new SubmitInteractionStatusError[] {});
        }
    }

    private static final Map<String, SubmitInteractionStatusError> createValuesMap() {
        Map<String, SubmitInteractionStatusError> map = new LinkedHashMap<>();
        map.put("error", ERROR);
        return map;
    }

    private static final Map<String, SubmitInteractionStatusErrorEnum> createEnumsMap() {
        Map<String, SubmitInteractionStatusErrorEnum> map = new HashMap<>();
        map.put("error", SubmitInteractionStatusErrorEnum.ERROR);
        return map;
    }
    
    
    public enum SubmitInteractionStatusErrorEnum {

        ERROR("error"),;

        private final String value;

        private SubmitInteractionStatusErrorEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

