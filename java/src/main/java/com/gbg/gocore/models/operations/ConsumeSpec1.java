/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gbg.gocore.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ConsumeSpec1 {

    public static final ConsumeSpec1 REQUIRED = new ConsumeSpec1("required");
    public static final ConsumeSpec1 OPTIONAL = new ConsumeSpec1("optional");
    public static final ConsumeSpec1 CONDITIONAL = new ConsumeSpec1("conditional");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ConsumeSpec1> values = createValuesMap();
    private static final Map<String, ConsumeSpec1Enum> enums = createEnumsMap();

    private final String value;

    private ConsumeSpec1(String value) {
        this.value = value;
    }

    /**
     * Returns a ConsumeSpec1 with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ConsumeSpec1
     */ 
    @JsonCreator
    public static ConsumeSpec1 of(String value) {
        synchronized (ConsumeSpec1.class) {
            return values.computeIfAbsent(value, v -> new ConsumeSpec1(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ConsumeSpec1Enum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ConsumeSpec1 other = (ConsumeSpec1) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ConsumeSpec1 [value=" + value + "]";
    }

    // return an array just like an enum
    public static ConsumeSpec1[] values() {
        synchronized (ConsumeSpec1.class) {
            return values.values().toArray(new ConsumeSpec1[] {});
        }
    }

    private static final Map<String, ConsumeSpec1> createValuesMap() {
        Map<String, ConsumeSpec1> map = new LinkedHashMap<>();
        map.put("required", REQUIRED);
        map.put("optional", OPTIONAL);
        map.put("conditional", CONDITIONAL);
        return map;
    }

    private static final Map<String, ConsumeSpec1Enum> createEnumsMap() {
        Map<String, ConsumeSpec1Enum> map = new HashMap<>();
        map.put("required", ConsumeSpec1Enum.REQUIRED);
        map.put("optional", ConsumeSpec1Enum.OPTIONAL);
        map.put("conditional", ConsumeSpec1Enum.CONDITIONAL);
        return map;
    }
    
    
    public enum ConsumeSpec1Enum {

        REQUIRED("required"),
        OPTIONAL("optional"),
        CONDITIONAL("conditional"),;

        private final String value;

        private ConsumeSpec1Enum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

