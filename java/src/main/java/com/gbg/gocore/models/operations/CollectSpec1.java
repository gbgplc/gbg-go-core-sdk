/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gbg.gocore.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class CollectSpec1 {

    public static final CollectSpec1 REQUIRED = new CollectSpec1("required");
    public static final CollectSpec1 OPTIONAL = new CollectSpec1("optional");
    public static final CollectSpec1 CONDITIONAL = new CollectSpec1("conditional");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, CollectSpec1> values = createValuesMap();
    private static final Map<String, CollectSpec1Enum> enums = createEnumsMap();

    private final String value;

    private CollectSpec1(String value) {
        this.value = value;
    }

    /**
     * Returns a CollectSpec1 with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as CollectSpec1
     */ 
    @JsonCreator
    public static CollectSpec1 of(String value) {
        synchronized (CollectSpec1.class) {
            return values.computeIfAbsent(value, v -> new CollectSpec1(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<CollectSpec1Enum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        CollectSpec1 other = (CollectSpec1) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "CollectSpec1 [value=" + value + "]";
    }

    // return an array just like an enum
    public static CollectSpec1[] values() {
        synchronized (CollectSpec1.class) {
            return values.values().toArray(new CollectSpec1[] {});
        }
    }

    private static final Map<String, CollectSpec1> createValuesMap() {
        Map<String, CollectSpec1> map = new LinkedHashMap<>();
        map.put("required", REQUIRED);
        map.put("optional", OPTIONAL);
        map.put("conditional", CONDITIONAL);
        return map;
    }

    private static final Map<String, CollectSpec1Enum> createEnumsMap() {
        Map<String, CollectSpec1Enum> map = new HashMap<>();
        map.put("required", CollectSpec1Enum.REQUIRED);
        map.put("optional", CollectSpec1Enum.OPTIONAL);
        map.put("conditional", CollectSpec1Enum.CONDITIONAL);
        return map;
    }
    
    
    public enum CollectSpec1Enum {

        REQUIRED("required"),
        OPTIONAL("optional"),
        CONDITIONAL("conditional"),;

        private final String value;

        private CollectSpec1Enum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

