/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gbg.gocore.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class StatusSuccess {

    public static final StatusSuccess SUCCESS = new StatusSuccess("success");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, StatusSuccess> values = createValuesMap();
    private static final Map<String, StatusSuccessEnum> enums = createEnumsMap();

    private final String value;

    private StatusSuccess(String value) {
        this.value = value;
    }

    /**
     * Returns a StatusSuccess with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as StatusSuccess
     */ 
    @JsonCreator
    public static StatusSuccess of(String value) {
        synchronized (StatusSuccess.class) {
            return values.computeIfAbsent(value, v -> new StatusSuccess(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<StatusSuccessEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        StatusSuccess other = (StatusSuccess) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "StatusSuccess [value=" + value + "]";
    }

    // return an array just like an enum
    public static StatusSuccess[] values() {
        synchronized (StatusSuccess.class) {
            return values.values().toArray(new StatusSuccess[] {});
        }
    }

    private static final Map<String, StatusSuccess> createValuesMap() {
        Map<String, StatusSuccess> map = new LinkedHashMap<>();
        map.put("success", SUCCESS);
        return map;
    }

    private static final Map<String, StatusSuccessEnum> createEnumsMap() {
        Map<String, StatusSuccessEnum> map = new HashMap<>();
        map.put("success", StatusSuccessEnum.SUCCESS);
        return map;
    }
    
    
    public enum StatusSuccessEnum {

        SUCCESS("success"),;

        private final String value;

        private StatusSuccessEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

