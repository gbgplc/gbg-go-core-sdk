# ScopeResponse

## Example Usage

```typescript
import { ScopeResponse } from "@gbg/go-core/models/operations";

let value: ScopeResponse = "desktop";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"mobile" | "desktop" | "*" | "any" | "" | Unrecognized<string>
```